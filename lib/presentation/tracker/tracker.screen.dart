import 'package:flutter/material.dart';

import 'package:get/get.dart';

import 'controllers/tracker.controller.dart';

class TrackerScreen extends GetView<TrackerController> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'TrackerScreen under construction',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}
